Index: git/qtmultimedia.pro
===================================================================
--- git.orig/qtmultimedia.pro
+++ git/qtmultimedia.pro
@@ -26,6 +26,7 @@ win32 {
                 GST_VERSION = 1.0
             }
         }
+    }
         cache(GST_VERSION, set)
         !isEmpty(GST_VERSION):qtCompileTest(gstreamer) {
             qtCompileTest(gstreamer_photography)
